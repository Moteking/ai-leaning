# 「専門家による無料相談」CTAのリンク先(クライアントに公開される)
NEXT_PUBLIC_CONSULT_CTA_URL=https://example.com/contact

# リード保存用 SQLite ファイルのパス(未指定なら ./data/leads.db)
# Vercel など書き込み不可の環境では /tmp 配下を指定するか、外部DB実装に差し替えてください。
# LEADS_DB_PATH=/tmp/leads.db
